accelerator:
  options:
    - name: blah
      label: blah
      description: blah blah blah
      dataType: string
      inputType: text
      required: true
      defaultValue: FFFFFF

engine:
  merge:
    # Modify workload.yaml java version environment variable.
    - include: ["catalog/catalog-info.yaml", "catalog/values.yaml", "catalog/mkdocs.yaml"]
      chain:
        - type: ReplaceText
          substitutions:
            - text: "default-project-name"          # workload 
              with: "#projectName"
            - text: "default-java-version"          # workload env var
              with: "#jvmVersion"
            - text: default-apps-namespace          # workload
              with: "#namespace"
            # - text: "default-java-version"          # workload env var
            #   with: "#jvmCatalogPatch"
        - type: YTT
